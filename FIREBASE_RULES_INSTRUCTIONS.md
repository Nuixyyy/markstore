# تعليمات تطبيق قواعد Firebase Security Rules

## الخطوات:

### 1. فتح Firebase Console
- اذهب إلى [Firebase Console](https://console.firebase.google.com/)
- اختر مشروعك

### 2. فتح Firestore Database
- من القائمة الجانبية، اختر **Firestore Database**
- اضغط على تبويب **Rules**

### 3. نسخ القواعد
- افتح ملف `firestore.rules` الذي تم إنشاؤه
- انسخ جميع المحتويات

### 4. لصق القواعد في Firebase
- الصق القواعد في مربع النص في Firebase Console
- اضغط على **Publish** لحفظ القواعد

## ملاحظات مهمة:

### ⚠️ قبل التطبيق:
1. **تأكد من معرف المطور الرئيسي**: 
   - المعرف الحالي: `NrXWrgBSsGfd9x8iTj8rvAQmoxI3`
   - إذا كان لديك معرف مختلف، قم بتحديثه في السطر:
   ```javascript
   function isMainDeveloper() {
     return isSignedIn() && request.auth.uid == 'YOUR_MAIN_DEVELOPER_UID';
   }
   ```

2. **إنشاء مستند الإعدادات**:
   - بعد تطبيق القواعد، يجب إنشاء مستند `settings/developers` في Firestore
   - البيانات المطلوبة:
   ```json
   {
     "uids": ["NrXWrgBSsGfd9x8iTj8rvAQmoxI3"]
   }
   ```

### 📋 ملخص الصلاحيات:

#### المنتجات (products):
- ✅ القراءة: الجميع
- ✅ الكتابة/التحديث/الحذف: المطورين فقط

#### الأقسام (categories):
- ✅ القراءة: الجميع
- ✅ الكتابة/التحديث/الحذف: المطورين فقط

#### التقييمات (reviews):
- ✅ القراءة: الجميع
- ✅ الكتابة: المستخدمين المسجلين
- ✅ التحديث/الحذف: المطورين أو صاحب التقييم

#### الملف الشخصي (users/{userId}/userProfile):
- ✅ القراءة: المستخدم نفسه أو المطورين
- ✅ الكتابة/التحديث: المستخدم نفسه فقط
- ✅ الحذف: المستخدم نفسه أو المطورين

#### السلة (users/{userId}/cart):
- ✅ جميع العمليات: المستخدم نفسه فقط

#### الطلبات (users/{userId}/orders):
- ✅ القراءة: المستخدم نفسه أو المطورين
- ✅ الكتابة: المستخدم نفسه فقط (عند إنشاء طلب)
- ✅ التحديث: المطورين فقط (لتحديث حالة الطلب)
- ✅ الحذف: المطورين فقط

#### المنتجات المميزة (featuredProducts):
- ✅ القراءة: الجميع
- ✅ الكتابة/التحديث/الحذف: المطورين فقط

#### إعدادات المطورين (settings/developers):
- ✅ القراءة: المطورين فقط
- ✅ الكتابة/التحديث: المطور الرئيسي فقط
- ❌ الحذف: غير مسموح

## اختبار القواعد:

بعد تطبيق القواعد، يمكنك اختبارها من Firebase Console:
1. اذهب إلى **Firestore Database** > **Rules**
2. اضغط على **Rules Playground**
3. اختبر السيناريوهات المختلفة

## استكشاف الأخطاء:

إذا واجهت مشاكل:
1. تأكد من أن المستخدم مسجل دخول (isSignedIn)
2. تأكد من وجود مستند `settings/developers`
3. تحقق من معرف المطور الرئيسي
4. راجع سجلات Firebase Console للأخطاء

